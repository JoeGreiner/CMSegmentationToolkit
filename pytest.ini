[pytest]
# Use the custom scored reporter defined in tests/conftest.py.
# -p no:terminal   suppress pytest default terminal reporter (dots / PASSED lines)
#                  so only the conftest output is shown.
# -s               disable output capture so print() inside tests is visible.
addopts = -p no:terminal -s
testpaths = tests
